import { NgModule } from '@angular/core';

@NgModule({
  // All components in this module are now standalone
  // This module is kept for backward compatibility but is not required
})
export class ProfileModule { }
